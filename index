.html -->
<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Todo App</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="style.css">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700&family=Shadows+Into+Light&display=swap" rel="stylesheet">

    <style>
        body {
            font-family: 'Poppins', sans-serif;
            background-color: #f8fafc;
            color: #334155;
        }
        .todo-container {
            backdrop-filter: blur(10px);
            background-color: rgba(255, 255, 255, 0.8);
            box-shadow: 0 10px 25px rgba(0, 0, 0, 0.05);
        }
        .gradient-text {
            background: linear-gradient(90deg, #3b82f6, #14b8a6);
            -webkit-background-clip: text;
            background-clip: text;
            color: transparent;
        }
        .todo-item {
            transition: all 0.3s ease;
        }
        .todo-item:hover {
            transform: translateX(5px);
        }
    </style>
</head>

<body class="min-h-screen flex flex-col items-center bg-gradient-to-br from-blue-50 to-teal-50 px-4">
    <h1 style="font-family:'Shadows Into Light', cursive;"
        class="text-4xl md:text-5xl lg:text-6xl font-semibold mt-16 md:mt-24 mb-8 gradient-text relative">
        Todo App
        <span class="absolute -bottom-2 left-1/2 transform -translate-x-1/2 h-1 w-3/4 bg-gradient-to-r from-blue-500 to-teal-400 rounded-full"></span>
    </h1>
    
    <div class="w-full sm:w-4/5 md:w-3/5 lg:w-2/5 p-6 md:p-8 m-2 rounded-xl shadow-lg todo-container">
        <form class="relative mb-6">
            <input id="todoInput" type="text"
                class="box-border w-full px-5 py-3 pr-28 bg-white/70 border-2 rounded-full border-gray-200 caret-blue-600 focus:outline-none focus:border-blue-500 focus:ring focus:ring-blue-200 transition-all duration-300 placeholder:text-gray-400"
                placeholder="Add a new task..." autocomplete="off">
            <button class="absolute top-1/2 -translate-y-1/2 right-2 rounded-full px-6 py-2 text-white font-medium
            bg-gradient-to-r from-blue-500 to-blue-600 hover:from-blue-600 hover:to-blue-700 transition-all duration-300 shadow-md" id="addBtn">
                Add
            </button>
        </form>
        
        <div class="mt-2 mb-4 flex justify-between items-center">
            <h2 class="text-lg font-medium text-gray-700">My Tasks</h2>
            <div class="text-sm text-gray-500" id="taskCount">0 tasks</div>
        </div>
        
        <ul id="todoList" class="gap-3 flex flex-col">
            <!-- Todo items will be inserted here by JavaScript -->
        </ul>
        
        <div class="mt-8 pt-4 border-t border-gray-200 flex justify-center">
            <button id="clearCompletedBtn" class="text-sm text-gray-500 hover:text-blue-600 transition-colors duration-300">
                Clear completed tasks
            </button>
        </div>
    </div>
    
    <footer class="mt-8 mb-4 text-sm text-center text-gray-500">
        Made with ❤️ | <span class="text-blue-500">2025</span>
    </footer>
</body>
<script src="script.js"></script>

</html>